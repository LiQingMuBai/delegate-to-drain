{
  "name": "eip7702-demo",
  "private": true,
  "workspaces": [
    "contracts",
    "web"
  ],
  "scripts": {
    "dev": "npm -w contracts run build && npm -w web run dev",
    "build": "npm -w contracts run build && npm -w web run build",
    "lint": "npm -w web run lint",
    "contracts:build": "npm -w contracts run build",
    "web:dev": "npm -w web run dev",
    "web:build": "npm -w web run build"
  }
}
